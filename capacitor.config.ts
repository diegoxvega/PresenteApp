import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyectoInicial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
